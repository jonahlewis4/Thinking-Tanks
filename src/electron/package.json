{
  "name": "thinking-tanks-gui",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc && cp src/index.html dist/",
    "start": "npm run build && electron .",
    "dev": "tsc && cp src/index.html dist/ && electron ."
  },
  "devDependencies": {
    "electron": "38.0.0-beta.6",
    "typescript": "^5.6.0"
  }
}
